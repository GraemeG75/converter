{
  "name": "converter.us",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx firebase-tools deploy",
    "deploy:hosting": "npm run build && npx firebase-tools deploy --only hosting"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  },
  "dependencies": {
    "firebase": "^12.17.1"
  }
}
